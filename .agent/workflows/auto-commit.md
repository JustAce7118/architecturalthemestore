---
description: Auto commit and push after execution
---
After completing any execution task, automatically commit and push changes:

// turbo
1. Stage all changes, commit with a descriptive message, and push:
```
git add . ; git commit -m "<descriptive message>" ; git push
```
Run this from the project root: `c:\Users\hp\.gemini\antigravity\scratch\shopify-theme-portfolio\01-architectural`
